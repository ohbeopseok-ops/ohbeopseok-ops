name: JoyLab Repository Control Tower (Legacy)

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "Legacy JoyLab Repository Control Tower is manual-only."
          echo "Daily scans are handled by Repository Control Tower V0.2."
