name: Repository Control Tower V0.1 (Legacy)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  pull-requests: read

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: V0.1 deprecated
        run: |
          echo "Repository Control Tower V0.1 is retained for manual historical diagnostics only."
          echo "Daily repository governance moved to Repository Control Tower V0.2."
